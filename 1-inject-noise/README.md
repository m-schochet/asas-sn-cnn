## This folder hosts all the jobs used for injecting the butterpy simulations into the template light curves for training set creation.

### These jobs should be run in the order: 1. create_folders, 2. inject_noise, 3. combine
