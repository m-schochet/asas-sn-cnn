This folder hosts all the jobs used for training the neural network. The model.py file holds the necessary CNN definitions for instantiating the network upon job submission.

These jobs should be run in the order: 1. cnn_train, 2. evaluate_training
